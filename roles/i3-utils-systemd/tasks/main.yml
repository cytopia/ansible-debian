---

- include_tasks: install.yml
  when: i3_utils_systemd == 'install'

- include_tasks: uninstall.yml
  when: i3_utils_systemd == 'remove'

# Make sure the handler runs now and not at the end of the playbook
- meta: flush_handlers
