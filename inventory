#
# Grouping based on gpu
#
# Customize:
# ----------
# 1. Copy group_vars/all.yml to host_vars/<name>.yml
# 2. Add your <name> to an existing or new group.
# 3. Run the following for your specific profile:
#
# ansible-playbook -i inventory playbook.yml --diff --limit <name> --ask-sudo-pass
#
[default]
localhost      ansible_connection=local

[lenovo-intel]
lenovo-i3      ansible_connection=local
