#
# Inventory file
#
# Grouping: <username>
# Naming:   <PREFIX>-<GPU>-<X>-<WM>
#
# Customize:
# ----------
# 1. Copy group_vars/all.yml to host_vars/<name>.yml
# 2. Customize host_vars/<name>.yml
# 3. Add your <name> to an existing or new group to this 'inventory' file.
# 4. Run the following for your specific profile:
#
# ansible-playbook -i inventory playbook.yml --diff --limit <name> --ask-sudo-pass


###
### Default example (everything enabled)
###
[default]
# $ ansible-playbook -i inventory playbook.yml --diff --limit localhost --ask-sudo-pass
localhost      ansible_connection=local


###
### cytopia's machines
###
[cytopia]
# $ ansible-playbook -i inventory playbook.yml --diff --limit intel-xorg-i3 --ask-sudo-pass
cytopia-intel-xorg-i3  ansible_connection=local
